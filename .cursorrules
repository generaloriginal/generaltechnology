NOTE: `.cursorrules` is the legacy Cursor rules file and is being deprecated by Cursor.

Rules in this repository have been migrated to:
- `AGENTS.md` (same folder)

If you need to update assistant rules, edit `AGENTS.md` instead.

